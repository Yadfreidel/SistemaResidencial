<UserControl x:Class="SistemaResidencial.Views.DashboardUsuarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <StackPanel Margin="8">
        <TextBlock Text="{Binding Bienvenida}" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <materialDesign:Card Margin="4" Padding="16" Visibility="{Binding TieneContrato, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <TextBlock Text="Mi contrato" FontWeight="Bold" Margin="0,0,0,8" />
                <TextBlock Text="{Binding InfoApartamento}" FontSize="14" Margin="0,0,0,4" />
                <TextBlock Margin="0,0,0,4">
                    <Run Text="Monto mensual: " />
                    <Run Text="{Binding MontoMensual, StringFormat={}{0:C}, Mode=OneWay}" FontWeight="Bold" />
                </TextBlock>
                <TextBlock>
                    <Run Text="Días para próximo pago: " />
                    <Run Text="{Binding DiasParaProximoPago, Mode=OneWay}" FontWeight="Bold" />
                </TextBlock>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Margin="4,16,4,4" Padding="16">
            <StackPanel>
                <TextBlock Text="Historial de pagos" FontWeight="Bold" Margin="0,0,0,8" />
                <DataGrid ItemsSource="{Binding HistorialPagos}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaPago, StringFormat=d}" Width="*" />
                        <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat={}{0:C}}" Width="*" />
                        <DataGridTextColumn Header="Método" Binding="{Binding MetodoPago}" Width="*" />
                        <DataGridTextColumn Header="Nº Recibo" Binding="{Binding NumeroRecibo}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </materialDesign:Card>
    </StackPanel>
</UserControl>
