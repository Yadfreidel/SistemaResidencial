<UserControl x:Class="SistemaResidencial.Views.DashboardRecepcionistaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <StackPanel Margin="8">
        <TextBlock Text="{Binding Bienvenida}" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <!-- ═══ Botones de navegación rápida ═══ -->
        <UniformGrid Columns="4" Rows="1" Margin="0,0,0,16">
            <Button Command="{Binding IrAApartamentosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                <StackPanel>
                    <TextBlock Text="🏢" FontSize="24" HorizontalAlignment="Center" />
                    <TextBlock Text="Apartamentos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Button>
            <Button Command="{Binding IrAInquilinosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                <StackPanel>
                    <TextBlock Text="👤" FontSize="24" HorizontalAlignment="Center" />
                    <TextBlock Text="Inquilinos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Button>
            <Button Command="{Binding IrAContratosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                <StackPanel>
                    <TextBlock Text="📄" FontSize="24" HorizontalAlignment="Center" />
                    <TextBlock Text="Contratos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Button>
            <Button Command="{Binding IrAPagosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                <StackPanel>
                    <TextBlock Text="💲" FontSize="24" HorizontalAlignment="Center" />
                    <TextBlock Text="Pagos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                </StackPanel>
            </Button>
        </UniformGrid>

        <UniformGrid Columns="2" Margin="0,0,0,16">
            <materialDesign:Card Margin="4" Padding="16">
                <StackPanel>
                    <TextBlock Text="Pagos pendientes" FontSize="13" Opacity="0.6" />
                    <TextBlock Text="{Binding TotalPendientes}" FontSize="28" FontWeight="Bold" Foreground="Red" />
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Margin="4" Padding="16">
                <StackPanel>
                    <TextBlock Text="Contratos por vencer" FontSize="13" Opacity="0.6" />
                    <TextBlock Text="{Binding TotalProximosVencer}" FontSize="28" FontWeight="Bold" />
                </StackPanel>
            </materialDesign:Card>
        </UniformGrid>

        <materialDesign:Card Margin="4" Padding="16">
            <StackPanel>
                <TextBlock Text="Contratos con pago pendiente este mes" FontWeight="Bold" Margin="0,0,0,8" />
                <DataGrid ItemsSource="{Binding PagosPendientes}" AutoGenerateColumns="False"
                          IsReadOnly="True" MaxHeight="220">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Inquilino" Binding="{Binding Inquilino.Nombre}" Width="*" />
                        <DataGridTextColumn Header="Apartamento" Binding="{Binding Apartamento.Numero}" Width="*" />
                        <DataGridTextColumn Header="Monto mensual" Binding="{Binding MontoMensual, StringFormat={}{0:C}}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Margin="4,16,4,4" Padding="16">
            <StackPanel>
                <TextBlock Text="Contratos próximos a vencer (30 días)" FontWeight="Bold" Margin="0,0,0,8" />
                <DataGrid ItemsSource="{Binding ContratosProximosVencer}" AutoGenerateColumns="False"
                          IsReadOnly="True" MaxHeight="220">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Inquilino" Binding="{Binding Inquilino.Nombre}" Width="*" />
                        <DataGridTextColumn Header="Apartamento" Binding="{Binding Apartamento.Numero}" Width="*" />
                        <DataGridTextColumn Header="Fecha fin" Binding="{Binding FechaFin, StringFormat=d}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </materialDesign:Card>
    </StackPanel>
</UserControl>
