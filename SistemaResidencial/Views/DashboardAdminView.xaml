<UserControl x:Class="SistemaResidencial.Views.DashboardAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <TextBlock Text="{Binding Bienvenida}" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

            <!-- ═══ Tarjetas de resumen ═══ -->
            <UniformGrid Columns="4" Rows="1" Margin="0,0,0,16">
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Apartamentos" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding TotalApartamentos}" FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Contratos activos" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding ContratosActivos}" FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Pagos del mes" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding PagosDelMes}" FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Morosos" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding Morosos}" FontSize="28" FontWeight="Bold" Foreground="Red" />
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>

            <!-- ═══ Sección de gráficos (resumen simple) ═══ -->
            <materialDesign:Card Margin="4" Padding="16">
                <StackPanel>
                    <TextBlock Text="Ocupación" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock>
                        <Run Text="Disponibles:" />
                        <Run Text="{Binding ApartamentosDisponibles, Mode=OneWay}" FontWeight="Bold" />
                        <Run Text="   Ocupados:" />
                        <Run Text="{Binding ApartamentosOcupados, Mode=OneWay}" FontWeight="Bold" />
                        <Run Text="   Ocupación:" />
                        <Run Text="{Binding PorcentajeOcupacion, StringFormat={}{0:0.0}%, Mode=OneWay}" FontWeight="Bold" />
                    </TextBlock>
                    <ProgressBar Value="{Binding PorcentajeOcupacion, Mode=OneWay}" Height="12" Margin="0,8,0,0" />
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Margin="4,16,4,4" Padding="16">
                <StackPanel>
                    <TextBlock Text="Ingresos por mes (últimos 6 meses)" FontWeight="Bold" Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding IngresoPorMes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Mes}" Grid.Column="0" />
                                    <TextBlock Text="{Binding Total, StringFormat={}{0:C}}" Grid.Column="1"
                                               FontWeight="Bold" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
