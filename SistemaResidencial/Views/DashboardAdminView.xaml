<UserControl x:Class="SistemaResidencial.Views.DashboardAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <TextBlock Text="{Binding Bienvenida}" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

            <!-- ═══ Botones de navegación rápida ═══ -->
            <UniformGrid Columns="4" Rows="1" Margin="0,0,0,16">
                <Button Command="{Binding IrAApartamentosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                    <StackPanel>
                        <TextBlock Text="🏢" FontSize="24" HorizontalAlignment="Center" />
                        <TextBlock Text="Apartamentos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding IrAInquilinosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                    <StackPanel>
                        <TextBlock Text="👤" FontSize="24" HorizontalAlignment="Center" />
                        <TextBlock Text="Inquilinos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding IrAContratosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                    <StackPanel>
                        <TextBlock Text="📄" FontSize="24" HorizontalAlignment="Center" />
                        <TextBlock Text="Contratos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding IrAPagosCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="4">
                    <StackPanel>
                        <TextBlock Text="💲" FontSize="24" HorizontalAlignment="Center" />
                        <TextBlock Text="Pagos" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0" />
                    </StackPanel>
                </Button>
            </UniformGrid>

            <!-- ═══ Tarjetas de resumen ═══ -->
            <UniformGrid Columns="4" Rows="1" Margin="0,0,0,16">
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Apartamentos" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding TotalApartamentos}" FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Contratos activos" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding ContratosActivos}" FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Pagos del mes" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding PagosDelMes}" FontSize="28" FontWeight="Bold" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Morosos" FontSize="13" Opacity="0.6" />
                        <TextBlock Text="{Binding Morosos}" FontSize="28" FontWeight="Bold" Foreground="Red" />
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>

            <!-- ═══ Sección de gráficos ═══ -->
            <UniformGrid Columns="2" Rows="1" Margin="0,0,0,16">
                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Ocupación" FontWeight="Bold" Margin="0,0,0,12" />
                        <ProgressBar Value="{Binding PorcentajeOcupacion, Mode=OneWay}" Maximum="100" Height="24" Margin="0,0,0,8" />
                        <TextBlock Text="{Binding PorcentajeOcupacion, StringFormat={}{0:F1}%, Mode=OneWay}" FontSize="14" HorizontalAlignment="Center" />
                        <TextBlock Margin="0,4,0,0" FontSize="12" Opacity="0.6">
                            <Run Text="{Binding ApartamentosOcupados, Mode=OneWay}" />
                            <Run Text="ocupados /" />
                            <Run Text="{Binding TotalApartamentos, Mode=OneWay}" />
                            <Run Text="total" />
                        </TextBlock>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Margin="4" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Ingresos últimos 6 meses" FontWeight="Bold" Margin="0,0,0,12" />
                        <ItemsControl ItemsSource="{Binding IngresoPorMes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="70" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Mes}" FontSize="11" VerticalAlignment="Center" />
                                        <ProgressBar Grid.Column="1" Height="16" Margin="8,0"
                                                     Maximum="{Binding DataContext.MaxIngreso, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                     Value="{Binding Total}" />
                                        <TextBlock Grid.Column="2" Text="{Binding Total, StringFormat={}{0:C}}" FontSize="11"
                                                   VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
